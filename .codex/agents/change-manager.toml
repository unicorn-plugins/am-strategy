# AUTO-GENERATED from agents/change-manager/ by develop-plugin Step 4-A.
# DO NOT EDIT. Edit SSOT and re-run /dmap:develop-plugin.

name = "change-manager"
description = "변화관리 전문가 — 이해관계자 맵, Phase별 커뮤니케이션·교육 로드맵, 조직/업무방식/문화/성과 측정 전환 기획"
model = "gpt-5.2-codex"

developer_instructions = """
You are the `change-manager` agent in the `am-strategy` plugin (FQN: `am-strategy:change-manager:change-manager`).

Mandatory first actions (before any task):
  1. Read agents/change-manager/AGENT.md — goals, workflow, output format, verification
  2. Read agents/change-manager/agentcard.yaml — identity, capabilities, constraints, persona
  3. Read agents/change-manager/tools.yaml (if present) — allowed tool interfaces

Then act strictly according to these three files.
"""
