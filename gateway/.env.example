# am-strategy 플러그인 커스텀 도구용 환경 변수
#
# 사용: `cp .env.example .env` 후 실제 키 입력
# .env 파일은 .gitignore에 포함됨 (커밋 금지)

# generate_image.py — Gemini Nano Banana 이미지 생성
GEMINI_API_KEY=

# convert-to-markdown.py — Groq VLM 이미지 설명
GROQ_API_KEY=
